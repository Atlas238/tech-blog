<section class = "card">
    <div class="card-header">
        <h2><a href='/blogposts/{{id}}'>{{title}}</a></h2>
        <div class="author">Posted by {{author_id}} on {{date}}</div>
    </div>
    <div class="card-body">
        <p>{{content}}</p>
    </div>
</section>